#include <iostream>

int main()
{
    std::cout << "=========LIGHTWEIGHT PROCESSES DEMOSTRATION=========" << std::endl;
    std::cout << "------------------------------------------" << std::endl;

    int numThread;
    std::cout << "Input number of threads in Thread Pool: ";
    std::cin >> numThread;
}